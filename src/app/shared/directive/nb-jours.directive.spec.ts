import { NbJoursDirective } from './nb-jours.directive';

describe('NbJoursDirective', () => {
  it('should create an instance', () => {
    const directive = new NbJoursDirective();
    expect(directive).toBeTruthy();
  });
});
